import { withProtected } from '@/layout/route';
import React from 'react';

type Props = {};

const KatalogPage = (props: Props) => {
    return <div>KatalogPage</div>;
};

export default withProtected(KatalogPage);
